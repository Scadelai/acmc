/* Test program for enhanced IR system */
int factorial(int n) {
    int result;
    result = 1;
    while (n > 0) {
        result = result * n;
        n = n - 1;
    }
    return result;
}

int main(void) {
    int num;
    int fact;
    num = 5;
    fact = factorial(num);
    output(fact);
    return 0;
}
