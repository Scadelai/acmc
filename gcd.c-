/* Um programa para calcular o mdc
   segundo o algoritmo de Euclides. */

int gcd (int u, int v)
{
	int temp;
	output(u/v);
	output(u/v*v);
	temp = u-u/v*v;
	output(temp);
	if (v == 0) return u ;
	else return gcd(v, temp);
	/* u-u/v*v == u mod v */
}

void main(void)
{	int x; int y;
	x = input();
	output (x);
	y = input();
	output (y);
	output(gcd(x,y));
}
