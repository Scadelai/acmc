/* Um programa para calcular o mdc
   segundo o algoritmo de Euclides. */

int gcd (int u, int v)
{       
	int a;
	int b;
	int c;
	output (u);
	output (v);
	a = u/v;
	output(a);
	b = a*v;
	output(b);
	c = u-b;
	output(c);
	if (v == 0) return u ;
	else return gcd(v,c);
	/* u-u/v*v == u mod v */
}

void main(void)
{	int x; int y;
	x = input(); y = input();
	output(gcd(x,y));
}